<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/frosted_background_white"
    tools:context=".activities.CourseDetailsActivity">

    <TextView
        android:id="@+id/text_detail_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:textSize="20sp"
        android:textStyle="bold"
        android:background="@color/frosted_background_light"
        android:textColor="@color/frosted_primary_dark" android:elevation="4dp"
        android:text="Loading Course Title..." />

    <ProgressBar
        android:id="@+id/progress_bar_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:indeterminate="true"
        android:indeterminateTint="@color/frosted_accent" android:layout_marginVertical="16dp"
        android:visibility="gone" />

    <WebView
        android:id="@+id/web_view_course_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>